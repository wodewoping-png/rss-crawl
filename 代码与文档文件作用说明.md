# 代码与文档文件作用说明

本文用于说明本仓库 `/home/ubuntu/work/rss-crawl` 下主要代码、文档、配置、数据目录和运行态文件的作用，方便后续维护、排查和交接。

## 1. 项目整体作用

本项目用于抓取 The Information 的订阅 RSS/Atom feed，将文章条目解析、清洗后输出为结构化 CSV，并按日、按周归档到 Git 仓库中。

核心链路如下：

```text
cron
  -> run_rss_job.sh
    -> automate_rss.py
      -> fetch_in0407.py
        -> The Information subscriber feed
      -> daily_csv/*.csv
      -> weekly_csv/*.csv
      -> AUTO_UPLOAD_LOG.md
      -> git add / commit / push origin main
```

## 2. 核心代码文件

| 文件 | 作用 | 维护要点 |
| --- | --- | --- |
| `fetch_in0407.py` | RSS 抓取与解析脚本。使用 Playwright 打开 `https://www.theinformation.com/subscriber_feed`，提取 Atom XML，解析标题、时间、作者、链接和正文，最后输出 CSV。 | 依赖 `state.json`、`THEINFO_USERNAME`、`THEINFO_PASSWORD`。正文清洗和截断规则在 `CONTENT_CUTOFF_MARKERS` 中维护。 |
| `automate_rss.py` | 自动化主流程脚本。负责按北京时间计算日任务和周任务日期，调用 `fetch_in0407.run_fetch()` 生成日 CSV，合并 7 个日 CSV 生成周 CSV，并按白名单提交和推送。 | 自动提交规则在 `is_auto_commit_allowed()` 中。默认推送到 `origin main`。可用 `--skip-git` 或 `--skip-push` 做演练。 |
| `pagetest.py` | 手动排障脚本。使用当前登录态和 Basic Auth 打开订阅 feed 页面，将页面内容写入 `debug_response.html`，并打印状态码、URL、标题等信息。 | 用于验证 `state.json` 和账号密码是否仍然可用，不是定时任务主链路的一部分。 |

## 3. 自动化入口与配置文件

| 文件 | 作用 | 维护要点 |
| --- | --- | --- |
| `run_rss_job.sh` | cron 调用入口。读取当前用户 shell 环境中的 `THEINFO_` 变量，检查账号密码是否存在，然后调用 `.venv/bin/python automate_rss.py --mode <daily|weekly>`。 | 如果 cron 里抓不到环境变量，优先检查 `.bashrc`、脚本里的 `load_theinfo_env()` 和 cron 日志。 |
| `rss_crawl.crontab` | 当前定时任务模板。配置每天运行日任务、每周五运行周任务。 | 当前时间按服务器 UTC 解释：`04:00 UTC` 对应北京时间 `12:00`，`05:00 UTC` 对应北京时间 `13:00`。 |
| `.gitignore` | Git 忽略规则。忽略虚拟环境、Python 缓存、调试 HTML、登录态和日志文件。 | `state.json`、`debug_response.html`、`logs/*.log` 不应进入仓库。 |

## 4. 文档文件

| 文件 | 作用 |
| --- | --- |
| `README_RSS_AUTOMATION.md` | 自动化使用指南，说明项目作用、依赖文件、定时任务规则、手动运行方式和常见问题排查。 |
| `项目流程说明.md` | 更完整的流程说明，按抓取、解析、日期过滤、日任务、周任务、自动提交、cron 行为和排查顺序描述项目链路。 |
| `AUTO_COMMIT_RULES.md` | 自动提交规则说明。约定 `.md`、`daily_csv/*.csv`、`weekly_csv/*.csv` 可由自动脚本提交，`.py` 和 `state.json` 不自动提交。 |
| `AUTO_UPLOAD_LOG.md` | 自动上传日志。记录每次日任务或周任务生成的目标日期、输出文件和运行时间。 |
| `代码与文档文件作用说明.md` | 本文件。用于汇总仓库中文件和目录的职责。 |

## 5. 数据输出文件与目录

| 路径 | 作用 | 说明 |
| --- | --- | --- |
| `daily_csv/` | 每日 CSV 输出目录。 | 文件名格式为 `YYYY-MM-DD.csv`。日期来自业务目标日期，抓取过滤时按 `America/Los_Angeles` 日期判断。 |
| `weekly_csv/` | 每周合并 CSV 输出目录。 | 文件名格式为 `YYYY-MM-DD_to_YYYY-MM-DD.csv`。由连续 7 个日 CSV 合并生成。 |
| `theinformation_feed.csv` | 手动运行 `fetch_in0407.py` 时的默认输出文件。 | 保存一次完整或指定条件下的 feed 抓取结果，不属于 cron 的日/周归档主输出。 |
| `logs/` | cron 运行日志目录。 | `logs/.gitkeep` 用于保留目录；`logs/*.log` 被 `.gitignore` 忽略，通常不提交。 |

CSV 表头固定为：

```text
标题,时间,作者,链接,内容
```

CSV 使用 `utf-8-sig` 编码写入，便于中文环境和常见表格软件打开。

## 6. 运行态、依赖与调试文件

| 文件或目录 | 作用 | 是否应提交 |
| --- | --- | --- |
| `state.json` | Playwright 登录态文件。抓取订阅 feed 时必须存在。 | 否。包含会话状态，已被 `.gitignore` 忽略。 |
| `debug_response.html` | 抓取失败或调试时保存的原始页面内容。 | 否。用于本地排查，已被 `.gitignore` 忽略。 |
| `.venv/` | Python 虚拟环境，cron 脚本通过其中的 Python 执行。 | 否。依赖环境不提交。 |
| `__pycache__/` | Python 运行生成的字节码缓存。 | 否。已忽略。 |
| `.codex` | Codex 本地运行相关文件。 | 否。已忽略。 |
| `.agents/` | 本地 agent/工具相关目录。 | 通常不提交，除非后续明确纳入项目配置。 |

## 7. Git 与推送相关文件行为

`automate_rss.py` 的自动提交不是简单提交所有改动，而是先读取 `git status --porcelain`，再按规则筛选。

允许自动提交：

- 所有 `.md` 文件
- `daily_csv/` 目录下的 `.csv` 文件
- `weekly_csv/` 目录下的 `.csv` 文件

禁止自动提交：

- 所有 `.py` 文件
- `state.json`

因此，自动任务生成 CSV 或更新文档时可以自动提交；代码变更需要人工明确提交，避免定时任务把未确认的代码改动推到远端。

## 8. 常见维护入口

| 场景 | 优先查看 |
| --- | --- |
| 日任务没有生成 CSV | `logs/daily.log`、`run_rss_job.sh`、`automate_rss.py` |
| 周任务没有生成 CSV | `logs/weekly.log`、`weekly_csv/`、`automate_rss.py` 的 `generate_weekly_csv()` |
| 抓取不到 feed | `state.json`、环境变量、`pagetest.py`、`debug_response.html` |
| CSV 内容格式不对 | `fetch_in0407.py` 的 `parse_atom()`、`html_to_text()`、`truncate_content()` |
| 自动提交或推送失败 | `AUTO_COMMIT_RULES.md`、`automate_rss.py` 的提交筛选逻辑、`git remote -v`、SSH 权限 |

## 9. 当前仓库远端

当前 Git 远端为：

```text
origin git@github.com:Qiaochu-Zhang/rss-crawl.git
```

自动脚本默认向 `origin main` 推送。
